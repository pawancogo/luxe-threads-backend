<% content_for :title, "Edit Admin - #{@admin.full_name}" %>

<div class="admin-card">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h1 class="card-title mb-0">
        <i class="fas fa-user-edit me-2"></i>Edit Admin
      </h1>
      <p class="text-muted mb-0 mt-2">Update admin information and permissions</p>
    </div>
    <%= link_to "Back", admin_admin_path(@admin), class: "btn btn-outline-secondary" %>
  </div>

  <%= render 'form', admin: @admin %>
</div>
