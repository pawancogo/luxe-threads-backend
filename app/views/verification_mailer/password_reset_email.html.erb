<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #f8f9fa; padding: 20px; text-align: center; border-radius: 8px; }
    .content { padding: 20px; }
    .password-box { background-color: #e9ecef; padding: 15px; border-radius: 5px; margin: 20px 0; text-align: center; }
    .button { display: inline-block; background-color: #007bff; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; margin: 20px 0; }
    .footer { text-align: center; margin-top: 30px; color: #666; font-size: 14px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Password Reset Request</h1>
    </div>
    
    <div class="content">
      <h2>Hello <%= @user.first_name %>!</h2>
      
      <% if @user_type == 'admin' %>
        <p>You have requested to reset your admin password for the LuxeThreads admin panel.</p>
      <% elsif @user_type == 'supplier' %>
        <p>You have requested to reset your supplier password for the LuxeThreads supplier portal.</p>
      <% else %>
        <p>You have requested to reset your password for your LuxeThreads account.</p>
      <% end %>
      
      <p>Please use the following temporary password to log in:</p>
      
      <div class="password-box">
        <strong>Temporary Password:</strong> <%= @temp_password %>
      </div>
      
      <p><strong>Important:</strong> You will be required to set a new permanent password after logging in with this temporary password.</p>
      
      <p>Click the button below to access your account:</p>
      
      <% if @user_type == 'admin' %>
        <a href="<%= admin_login_url %>" class="button">Access Admin Panel</a>
      <% elsif @user_type == 'supplier' %>
        <a href="<%= supplier_login_url %>" class="button">Access Supplier Portal</a>
      <% else %>
        <a href="<%= user_login_url %>" class="button">Access My Account</a>
      <% end %>
      
      <p>If you did not request this password reset, please ignore this email and contact our support team immediately.</p>
      
      <p>Thank you for using LuxeThreads!</p>
    </div>
    
    <div class="footer">
      <p>This email was sent to <%= @user.email %></p>
      <p>© 2024 LuxeThreads. All rights reserved.</p>
    </div>
  </div>
</body>
</html>


