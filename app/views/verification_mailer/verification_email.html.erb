<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #f8f9fa; padding: 20px; text-align: center; border-radius: 8px; }
    .content { padding: 20px; }
    .password-box { background-color: #e9ecef; padding: 15px; border-radius: 5px; margin: 20px 0; text-align: center; }
    .button { display: inline-block; background-color: #007bff; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; margin: 20px 0; }
    .footer { text-align: center; margin-top: 30px; color: #666; font-size: 14px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Welcome to LuxeThreads!</h1>
    </div>
    
    <div class="content">
      <h2>Hello <%= @user.first_name %>!</h2>
      
      <% if @user_type == 'admin' %>
        <p>Your admin account has been created successfully. You now have access to the LuxeThreads admin panel.</p>
      <% elsif @user_type == 'supplier' %>
        <p>Welcome to the LuxeThreads Supplier Portal! Your supplier account has been created successfully.</p>
      <% else %>
        <p>Welcome to LuxeThreads! Your customer account has been created successfully.</p>
      <% end %>
      
      <p>To complete your account setup, please use the following temporary password to log in:</p>
      
      <div class="password-box">
        <strong>Temporary Password:</strong> <%= @temp_password %>
      </div>
      
      <p><strong>Important:</strong> You will be required to set a new permanent password after logging in with this temporary password.</p>
      
      <p>Click the button below to access your account:</p>
      
      <% if @user_type == 'admin' %>
        <a href="<%= admin_login_url %>" class="button">Access Admin Panel</a>
      <% elsif @user_type == 'supplier' %>
        <a href="<%= supplier_login_url %>" class="button">Access Supplier Portal</a>
      <% else %>
        <a href="<%= user_login_url %>" class="button">Access My Account</a>
      <% end %>
      
      <p>If you have any questions or need assistance, please don't hesitate to contact our support team.</p>
      
      <p>Thank you for choosing LuxeThreads!</p>
    </div>
    
    <div class="footer">
      <p>This email was sent to <%= @user.email %></p>
      <p>© 2024 LuxeThreads. All rights reserved.</p>
    </div>
  </div>
</body>
</html>


